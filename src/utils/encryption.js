
// import { AES, enc } from 'crypto-js';
// const secretKey= process.env.SECRET_KEY
// export const encryptParams = (params) => {
//     const encryptedString = AES.encrypt(JSON.stringify(params), secretKey).toString();
//     return encodeURIComponent(encryptedString);
//   };
  
//   export const decryptParams = (encryptedParams) => {
//     const decryptedString = AES.decrypt(decodeURIComponent(encryptedParams), secretKey).toString(enc.Utf8);
//     return JSON.parse(decryptedString);
//   };
  